import { MyMatPaginatorIntl } from './my-mat-paginator-intl';

describe('MyMatPaginatorIntl', () => {
  it('should create an instance', () => {
    expect(new MyMatPaginatorIntl()).toBeTruthy();
  });
});
