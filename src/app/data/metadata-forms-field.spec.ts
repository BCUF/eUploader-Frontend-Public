import { MetadataFormsField } from './metadata-forms-field';

describe('MetadataFormsField', () => {
  it('should create an instance', () => {
    expect(new MetadataFormsField()).toBeTruthy();
  });
});
